.claude
CLAUDE.md
# ---------------------------------------------------------------
# .NET backend (FitBook.Api / Worker / Services / Repository / Domain)
# ---------------------------------------------------------------

# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.suo
*.user
*.userosscache
*.sln.docstates
*.csproj.user
*.swp

# Local config & secrets
appsettings.Development.json
appsettings.Local.json
appsettings.*.local.json
*.pfx
secrets.json

# Test & coverage
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
coverage*.info
*.trx
*.coverage

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!packages/build/

# Misc
*.dll.config
*.pdb
.DS_Store

# ---------------------------------------------------------------
# Flutter apps (fitbook_desktop / fitbook_mobile)
#
# Each app carries its own .gitignore for the standard Flutter
# artifacts (build/, .dart_tool/, symbols). Only the extras go here.
#
# The bundled Inter font files under */assets/fonts/ ARE committed
# on purpose - both apps must render correctly offline during the
# review, so the typeface cannot be a runtime download.
# ---------------------------------------------------------------

# Golden-test failure artifacts
**/test/failures/

# Generated platform scaffolding not covered per-app
**/android/.gradle/
**/android/local.properties
**/windows/flutter/ephemeral/

# fitbook_mobile targets Android only, the way fitbook_desktop targets Windows
# only. `flutter create` regenerates ios/ on a Mac; it is not a build target,
# so it stays out of the repo.
fitbook_mobile/ios/
